# cpp_tutorial


Example

Navigate to the oop_person.

Make directory to build the program.

`mkdir build`

Then, navigate to the build directory.

`cd /build`

Command cmake.

`cmake ..`

Build it.

`make`

Execute the program

`./main`